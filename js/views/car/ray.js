import Point from "./point.js";

class Ray {
    /**
     * @param {Point} start 
     * @param {Point} end 
     */
    constructor(start, end) {
        this.start = start;
        this.end = end;
    }
}

export default Ray;
