class Intersection {
    /**
     * @param {number} x 
     * @param {number} y 
     * @param {number} offset 
     */
    constructor(x, y, offset) {
        this.x = x;
        this.y = y;
        this.offset = offset;
    }
}

export default Intersection;
